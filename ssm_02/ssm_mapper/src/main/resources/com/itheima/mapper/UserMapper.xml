<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.UserMapper">

    <select id="findUser" resultType="user">
        select * from user where username=#{username} and password=#{password}
    </select>

    <select id="findAll" resultType="user">
        select * from user
        <where>
            <if test="user!=null">
                <if test="user.name!=null and user.name.length>0">
                    and name like '%${user.name}%'
                </if>
                <if test="user.address!=null and user.address.length>0">
                    and address like '%${user.address}%'
                </if>
                <if test="user.email!=null and user.email.length>0">
                    and email like '%${user.email}%'
                </if>
            </if>
        </where>
        limit #{start},#{pageSize}
    </select>

    <select id="findByPic" parameterType="int" resultType="String">
        select pic from user where id=#{id}
    </select>
    
    <select id="findById" parameterType="int" resultType="user">
        select * from user where id=#{id}
    </select>

    <delete id="deleteById" parameterType="int">
        delete from user where id=#{id}
    </delete>

    <update id="updateUser" parameterType="user">
        update user set name = #{name},gender = #{gender} ,age = #{age} , address = #{address} , qq = #{qq}, email = #{email},pic=#{pic} where id = #{id}
    </update>

    <insert id="saveUser" parameterType="user">
        insert into user values(id,#{name},#{gender},#{age},#{address},#{qq},#{email},username,password,#{pic})
    </insert>

    <select id="findTotalCount" parameterType="user" resultType="int">
        SELECT count(*) from user
        <where>
                <if test="name!=null and name.length>0">
                    and name like '%${name}%'
                </if>
                <if test="address!=null and address.length>0">
                    and address like '%${address}%'
                </if>
                <if test="email!=null and email.length>0">
                    and email like '%${email}%'
                </if>
        </where>
    </select>

    
</mapper>